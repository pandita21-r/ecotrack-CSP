# 1. Fixed MySQL Connection URL
spring.datasource.url=jdbc:mysql://localhost:3306/ecotrack_db?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=virche22
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 2. Keep the server port separate (if you still want to use 8081)
server.port=8080

# 3. Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true